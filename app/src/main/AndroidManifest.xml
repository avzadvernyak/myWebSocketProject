<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="kampukter.myWebSocketProject">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning"
            android:name=".UnitSensorApplication">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service android:name=".WebSocketService"/>
        <service android:name=".FetchAddressIntentService"
                 android:exported="false">
        </service>

        <activity android:name=".ui.SettingsActivity"
                  android:parentActivityName=".ui.MainActivity"
                  android:label="SettingsActivity">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".ui.MainActivity">
            </meta-data>
        </activity>
        <activity android:name=".ui.Sensor1FullScreenGraph"
                  android:parentActivityName=".ui.MainActivity"
                  android:label="SettingsActivity"
        >
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".ui.MainActivity">
            </meta-data>
        </activity>
        <activity android:name=".ui.Sensor1Information"
                  android:parentActivityName=".ui.MainActivity"
                  android:label="Sensor1Information"
        >
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".ui.MainActivity">
            </meta-data>
        </activity>
        <activity android:name=".ui.LocationActivity"/>
    </application>

</manifest>